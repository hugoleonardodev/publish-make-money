import styled from 'styled-components';

export const CompanySymbol = styled.h3`
  margin: 0;
  padding-left: 8px;
`;

export const CompanyTitles = styled.h5`
  margin: 0;
  opacity: 0.5;
  padding-left: 8px;
`;
